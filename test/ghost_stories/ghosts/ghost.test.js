const { assert } = require('chai');

const Ghost = require('../../../ghost_stories/ghosts/ghost');

describe('Ghost', () => {
  describe('moveHauntingFigure', () => {
    it('should ', () => {

    });
  });
});
